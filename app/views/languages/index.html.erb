<%- model_class = Language -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
			<th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= "ISO" %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
			<th><%= "Trans. #" %></th>
      <th><%=t '.actions', :default => t("helpers.managing") %></th>
    </tr>
  </thead>
  <tbody>
    <% @languages.each do |language| %>
      <tr>
        <td><%= language.id %></td>
				<td><%= link_to language.name, language_path(language) %></td>
        <td><%= language.iso %></td>
        <td><%=l language.created_at %></td>
				<td><%= language.translations.size %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_language_path(language), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.del")),
                      language_path(language),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_language_path,
            :class => 'btn btn-primary' %>
